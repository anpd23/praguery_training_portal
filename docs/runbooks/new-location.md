# Runbook: new location

1. Add a `locations` row: name, type (`cafe` | `food_truck` | `catering_truck`), slug, address, hours, organization.
2. Attach checklists by `location_type` or clone a site-specific template. Number them `SCOPE-AREA-CNNN`.
3. Create the first manager and supervisor profiles for that site.
4. Provision the iPad: Safari → Add to Home Screen → Guided Access optional.
5. Confirm opening/closing checklists appear for that location slug.
6. Walk one trainee through role tap → home → a module → a checklist while online, then with Wi-Fi off.
